  // Equal to (==)
    let a = 5;
    let b = '5';
    console.log("a == b:", a == b); // true

    // Strict Equal to (===)
    console.log("a === b:", a === b); // false

    // Not Equal to (!=)
    let x = 10;
    let y = 20;
    console.log("x != y:", x != y); // true

    // Strict Not Equal to (!==)
    let z = '10';
    console.log("x !== z:", x !== z); // true

    // Greater Than (>)
    let age = 21;
    console.log("age > 18:", age > 18); // true

    // Less Than (<)
    let score = 70;
    console.log("score < 100:", score < 100); // true

    // Greater Than or Equal To (>=)
    let marks = 60;
    console.log("marks >= 60:", marks >= 60); // true

    // Less Than or Equal To (<=)
    let speed = 80;
    console.log("speed <= 90:", speed <= 90); // true
  
