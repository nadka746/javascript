 // Logical AND (&&)
    let age = 22;
    let hasID = true;
    console.log("age >= 18 && hasID:", age >= 18 && hasID); // true

    // Logical OR (||)
    let isStudent = false;
    let hasDiscountCard = true;
    console.log("isStudent || hasDiscountCard:", isStudent || hasDiscountCard); // true

    // Logical NOT (!)
    let isOnline = false;
    console.log("!isOnline:", !isOnline); // true

    // Combined Logic
    let loggedIn = true;
    let isAdmin = false;
    console.log("loggedIn && !isAdmin:", loggedIn && !isAdmin); // true 